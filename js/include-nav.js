// Deprecated: the nav is now inlined directly into each prototype page so the
// pages work when opened with file:// (no fetch/server needed). This file is
// kept as a no-op so any stale <script src="js/include-nav.js"> tag still works.
// To edit the nav, update the inline <nav class="halo-navigation">…</nav>
// block in each page, or copy partials/nav.html and re-inline.
